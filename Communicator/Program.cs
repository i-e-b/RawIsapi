namespace Communicator
{
    public class Program
    {
        public static int Main(string[] args)
        {
            // Prove that we can load an exe as if it were a dll
            return 0;
        }
    }
}